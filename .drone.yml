kind: pipeline
type: exec
name: serhii-vysotskyi

steps:
  - name: setup
    commands:
      - echo "Setting up the environment"
      - whoami
      - apt update

  - name: build
    commands:
      - echo "Building the project"

  - name: test
    commands:
      - echo "Running tests"
