kind: pipeline
type: exec
name: serhii-vysotskyi

steps:
  - name: setup
    commands:
      - bash check_docker.sh

  - name: build
    commands:
      - echo "Building the project"

  - name: test
    commands:
      - echo "Running tests"
