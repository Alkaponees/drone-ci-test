kind: pipeline
type: exec
name: serhii-vysotskyi

steps:
  - name: setup
    commands:
      - if command -v docker &> /dev/null; then
          echo "Docker is installed."
        else
          "Docker is not installed. Start installation "
           pwd
        fi

  - name: build
    commands:
      - echo "Building the project"

  - name: test
    commands:
      - echo "Running tests"
